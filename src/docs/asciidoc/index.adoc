= Example using netflux with Springboot

Celem aplikacji jest zapoznanie się z programowaniem reaktywnym

== Inicjalizacja aplikacji jakimiś danymi

.Inicjlaizacja aplikacji
[source ,java]
----
include::{sourcedir}/guru/springframework/netfluxexample/bootstrap/BootstrapCLR.java[]
----
<0> Jak tylko usuniesz to ...
<1> Lista jakiś tytułów filmów, troche zmienionych by wiązały się trochę z tematem
<2> Tworzymy strumień obiektów Movie na podstawie tytułów
<3> flatMap ponieważ `save` tworzy nowy strumień
<4>  subscribe jak się zakończy to wykonaj lambdę